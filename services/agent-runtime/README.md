# OmniClaw Agent Runtime Placeholder

Phase 1 keeps runtime execution behind the TypeScript `RuntimeAdapter` interface and uses a deterministic mock.
This `uv` project is intentionally empty so later phases can add LangGraph, LangChain, model providers, and sandbox execution without changing the API state machine.
